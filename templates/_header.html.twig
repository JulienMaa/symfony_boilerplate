<header class="masthead mb-auto text-center bg-4">
    <div class="inner">
    <h1 class="masthead-brand text-3 font-weight-bold">Boiler Plate</h1>
    <nav class="nav nav-masthead justify-content-center">
        <a class="nav-link text-2" href="{{ path('home') }}">Home</a>
        <a class="nav-link text-2" href="{{ path('task_index') }}">Tasks List</a>
        {% if app.user %}
            <a class="nav-link text-2" href="{{ path('app_logout') }}">Logout</a>
        {% else %}
            <a class="nav-link text-2" href="{{ path('app_login') }}">Login</a>
        {% endif %}
    </nav>
    </div>
</header>